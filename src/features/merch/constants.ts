// Merch pricing rules shared by the storefront UI. Server recalculates totals authoritatively.
export const SHIPPING_CENTS = 700;
export const DEFAULT_SIZES = ["S", "M", "L", "XL", "2XL", "3XL"] as const;
